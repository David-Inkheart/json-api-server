{
  "name": "json-api-server",
  "version": "1.0.0",
  "description": "Template for creating a backend API server using json-server with ready-to-deploy config for vercel",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "dev": "NODE_ENV=development node server.js",
    "start": "NODE_ENV=production node server.js",
    "watch": "json-server db.json --port=8000"
  },
  "dependencies": {
    "clone": "^2.1.2",
    "json-server": "^0.17.3"
  }
}